import { InteractableDirective } from './interactable.directive';

describe('InteractableDirective', () => {
  it('should create an instance', () => {
    const directive = new InteractableDirective();
    expect(directive).toBeTruthy();
  });
});
